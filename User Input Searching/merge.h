#pragma once
#ifndef MERGE_H
#define MERGE_H
#include <vector>

std::vector<int> merge_sort(std::vector<int> vin);
std::vector<int> merging(std::vector<int> vina, std::vector<int> vinb);

#endif